route:
  receiver: jenkins-rollback-webhook
  group_wait: 0s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: jenkins-rollback-webhook
    webhook_configs:
      - url: 'http://20.244.49.37:8080/generic-webhook-trigger/invoke?token=ROLLBACK_E70037_TOKEN'
        send_resolved: false
        http_config:
          basic_auth:
            username: 'admin'
            password: 'JENKINS_API_TOKEN_PLACEHOLDER'
